import React from 'react';
import { View } from 'react-native';

const DateTimePicker = ({ value, mode, onChange, ...props }) => {
  // Mock implementation
  return <View testID="datetime-picker" {...props} />;
};

export default DateTimePicker;