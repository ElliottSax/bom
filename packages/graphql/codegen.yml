overwrite: true
schema: "./schema.graphql"
generates:
  src/generated/types.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
    config:
      skipTypename: false
      withHooks: true
      withHOC: false
      withComponent: false
      scalars:
        DateTime: string
